<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="si.uni_lj.fe.tnuv.wallettracker">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WalletTracker"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.DashboardActivity"
            android:exported="true"
            android:label="Wallet Tracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AddEntryActivity"
            android:exported="false"
            android:label="Dodaj vnos" />
        <activity
            android:name=".activity.ChartActivity"
            android:exported="false"
            android:label="Graf zgodovine" />
    </application>
</manifest>
